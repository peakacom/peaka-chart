apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "peaka.fullname" . }}-be-token-service
  labels:
    app.kubernetes.io/name: {{ include "peaka.fullname" . }}-be-token-service
    {{- include "peaka.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.tokenService.replicaCount }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "peaka.fullname" . }}-be-token-service
      {{- include "peaka.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "peaka.fullname" . }}-be-token-service
        {{- include "peaka.labels" . | nindent 8 }}
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/env-configmap.yaml") . | sha256sum }}
    spec:
      imagePullSecrets:
        {{- if .Values.peakaContainerRegistryAccessSecret }}
        - name: {{ .Values.peakaContainerRegistryAccessSecret.name }}
        {{- end }}
        {{- range $_, $secret := .Values.imagePullSecrets }}
        - name: {{ $secret }}
        {{- end }}
      initContainers:
      {{- if eq (include "peaka.customCA.importEnabled" .) "true" }}
        {{- include "peaka.customCA.initContainer.java" (dict "image" (printf "%s/%s:%s" (default (include "peaka.image.registry" .) .Values.tokenService.image.registry) .Values.tokenService.image.name .Values.tokenService.image.tag)) | nindent 8 }}
      {{- end }}
      containers:
        - name: token-service
          image: {{ default (include "peaka.image.registry" .) .Values.tokenService.image.registry }}/{{ .Values.tokenService.image.name }}:{{ .Values.tokenService.image.tag }}
          imagePullPolicy: {{ default "IfNotPresent" .Values.tokenService.image.imagePullPolicy }}
          ports:
            - name: http
              containerPort: 8080
          envFrom:
            - configMapRef:
                name: {{ include "peaka.fullname" . }}-env-config
          {{- with .Values.tokenService.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tokenService.readinessHealthGroup }}
          env:
            - name: MANAGEMENT_ENDPOINT_HEALTH_GROUP_READINESS_INCLUDE
              value: {{ . | quote }}
          {{- end }}
          {{- with .Values.tokenService.startupProbe }}
          startupProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tokenService.livenessProbe }}
          livenessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.tokenService.readinessProbe }}
          readinessProbe:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          volumeMounts:
          {{- if eq (include "peaka.customCA.javaEnabled" .) "true" }}
            - name: custom-ca-truststore
              mountPath: /truststore
              readOnly: true
          {{- end }}
      {{- if .Values.tokenService.terminationGracePeriodSeconds }}
      terminationGracePeriodSeconds: {{ .Values.tokenService.terminationGracePeriodSeconds }}
      {{- end }}
      volumes:
      {{- if eq (include "peaka.customCA.importEnabled" .) "true" }}
        - name: custom-ca-certs
          configMap:
            name: {{ include "peaka.fullname" . }}-custom-ca-certs
        - name: custom-ca-truststore
          emptyDir: {}
      {{- else if eq (include "peaka.customCA.javaTruststoreProvided" .) "true" }}
        # Pre-built truststore from values (hardened clusters block the
        # keytool import's emptyDir); mounted read-only.
        - name: custom-ca-truststore
          secret:
            secretName: {{ include "peaka.fullname" . }}-custom-ca-java-truststore
      {{- end }}
      {{- include "peaka.common.nodeSelector" ( dict "nodeSelector" .Values.tokenService.nodeSelector "global" $.Values.global) | nindent 6 }}
      {{- with .Values.tokenService.affinity }}
      affinity:
      {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "peaka.common.tolerations" ( dict "tolerations" .Values.tokenService.tolerations "global" $.Values.global) | nindent 6 }}
